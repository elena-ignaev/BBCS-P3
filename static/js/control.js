//pic1, pic2, pic3 - pictures
function search() {
    /*
    take the html file you want to redirect to as the parameter: window.location.replace() */
    var request = document.getElementById("seach-textbox").value;
    // in the form of ?query=whatever window.location.replace();
}

